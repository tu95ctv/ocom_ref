<odoo>
  
    <record id="tp_sale_list" model="ir.ui.view">
        <field name="name">tp.sale.order.list</field>
        <field name="model">tp.sale.order</field>
        <field name="inherit_id" ref="tp_sale.tp_sale_list"></field>
        <field name="arch" type="xml">
            <!-- <tree>
                <field name="name" />
                <field name="customer_id" />
                <field name="order_date" />
            </tree> -->
            <field name="order_date" position="after">
                <field name="write_date" />
            </field>
        </field>
    </record>

</odoo>